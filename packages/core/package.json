{
  "name": "@age-of-hero/core",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --fix && tsc --noEmit",
    "build": "tsc",
    "ncu": "ncu -u"
  },
  "dependencies": {
    "@age-of-hero/schemas": "workspace:*"
  },
  "devDependencies": {
    "@age-of-hero/eslint-config-custom": "workspace:*",
    "@age-of-hero/typescript-config": "workspace:*"
  }
}
